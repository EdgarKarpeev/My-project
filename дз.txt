import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class FactorialTest {
    
    @Test
    public void testFactorialPositive() {
        assertEquals(120, Factorial.calculate(5));
        assertEquals(1, Factorial.calculate(0));
        assertEquals(1, Factorial.calculate(1));
    }
    
    @Test
    public void testFactorialNegative() {
        assertThrows(IllegalArgumentException.class, () -> Factorial.calculate(-1));
    }
}

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class TriangleAreaTest {
    
    @Test
    public void testValidTriangle() {
        assertEquals(6.0, TriangleArea.calculate(3, 4, 5), 0.001);
        assertEquals(1.984, TriangleArea.calculate(2, 3, 4), 0.001);
    }
    
    @Test
    public void testInvalidTriangle() {
        assertThrows(IllegalArgumentException.class, () -> TriangleArea.calculate(1, 2, 3));
    }
}

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class CalculatorTest {
    
    @Test
    public void testAddition() {
        assertEquals(5, Calculator.calculate(2, 3, '+'));
    }
    
    @Test
    public void testSubtraction() {
        assertEquals(-1, Calculator.calculate(2, 3, '-'));
    }
    
    @Test
    public void testMultiplication() {
        assertEquals(6, Calculator.calculate(2, 3, '*'));
    }
    
    @Test
    public void testDivision() {
        assertEquals(1.5, Calculator.calculate(3, 2, '/'), 0.001);
    }
    
    @Test
    public void testDivisionByZero() {
        assertThrows(ArithmeticException.class, () -> Calculator.calculate(3, 0, '/'));
    }
    
    @Test
    public void testInvalidOperation() {
        assertEquals("Ошибка: неверная операция", Calculator.calculate(3, 2, '%'));
    }
}

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class CompareNumbersTest {
    
    @Test
    public void testFirstGreater() {
        assertEquals("5 больше 3", CompareNumbers.compare(5, 3));
    }
    
    @Test
    public void testSecondGreater() {
        assertEquals("3 меньше 5", CompareNumbers.compare(3, 5));
    }
    
    @Test
    public void testEqualNumbers() {
        assertEquals("4 равно 4", CompareNumbers.compare(4, 4));
    }
}